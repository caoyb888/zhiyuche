{
  "name": "zhiyuche",
  "private": true,
  "version": "0.1.0",
  "description": "智御车辆智能租赁与全生命周期管理系统 monorepo",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "web:dev": "npm run dev -w apps/web",
    "web:build": "npm run build -w apps/web",
    "web:check": "npm run typecheck -w apps/web && npm run lint -w apps/web"
  }
}
